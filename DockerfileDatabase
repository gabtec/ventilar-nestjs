FROM postgres:15.1-alpine as build-db-stage

ENV POSTGRES_USER=admin
ENV POSTGRES_PASSWORD=admin
ENV POSTGRES_DB=ventilar_db

COPY ./deploy/db/ /etc/docker-entrypoint-initdb.d/

# CMD ["-c", "config_file=/etc/postgresql/postgresql.conf"]